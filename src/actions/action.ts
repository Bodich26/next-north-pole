"use server";
export async function updateGiftStatus(giftId: string, nextStatus: string) {
  // Логика обновления статуса подарка на сервере
  console.log(`Updating gift ${giftId} to status ${nextStatus}`);
}
